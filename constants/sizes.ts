/**
 * Application Dimension Constants
 *
 * Central repository for shared dimensional values used across the application's UI.
 * This ensures consistency in layout elements, particularly for complex components
 * like tooltips where precise pixel measurements are required for alignment across
 * different platforms (iOS/Android).
 */
export const Sizes = {
  tooltipWidth: 230,
  tooltipHeight: 44,
  tooltipArrowSize: 6,
  tooltipIosPadding: 72,
  tooltipAndroidPadding: 47,
  tooltipScreenPadding: 10,
};
